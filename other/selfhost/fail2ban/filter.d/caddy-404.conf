# Fail2ban filter for Caddy 404 scanning
# Detects repeated 404 responses (vulnerability scanners)
# Ignores legitimate app 404s (Immich thumbnails, etc.)

[Definition]
failregex = ^.*"remote_ip":"<HOST>".*"status":404.*$
ignoreregex = /api/assets/.*/thumbnail
datepattern = {^LN-BEG}"ts":{EPOCH}
