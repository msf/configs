# Fail2ban filter for Caddy 404 scanning
# Detects repeated 404 responses (vulnerability scanners)

[Definition]
failregex = ^.*"status":\s*404.*"remote_ip":\s*"<HOST>".*$
ignoreregex =
datepattern = "ts":\s*%%s\.
