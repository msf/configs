# Fail2ban filter for Caddy authentication failures
# Detects 401/403 responses in Caddy JSON logs

[Definition]
failregex = ^.*"status":\s*(401|403).*"remote_ip":\s*"<HOST>".*$
ignoreregex =
datepattern = "ts":\s*%%s\.
