---
description: Analyze recent sessions for patterns and propose workflow improvements
---

Load and execute the `weekly-review` skill.

- Call `skill` with name `weekly-review`.
- If `$ARGUMENTS` is provided, pass it as the day range (default: 7).
