---
description: Load and execute the send-pr skill
---

Load and execute the `send-pr` skill.

- Call `skill` with name `send-pr`.
- If `$ARGUMENTS` is provided, pass it as the PR title override to the skill.
