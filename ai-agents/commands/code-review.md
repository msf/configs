---
description: Code Review a PR, branch, or diff using the code-review skill
---

Load and execute the `code-review` skill.

- Call `skill` with name `code-review`.
- `$ARGUMENTS` is the PR number, URL, or branch to review. If empty, ask for PR urls
